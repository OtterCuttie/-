#include <iostream>

using namespace std;

int main()
{
	long long n;
	long long m;
	cin >> n;
	cin >> m;
	if (m > 0 and n > 0) {
		cout << (m + 1) * 2 * n;
	}
	else if (m == 0) {
		cout << n*2;
	}
	else if (n == 0) {
		cout << m+1;
	}
}
